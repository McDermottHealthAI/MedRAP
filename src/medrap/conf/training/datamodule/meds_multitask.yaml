_target_: medrap.train.multitask_datamodule.MultiTaskMEDSDatamodule

config:
  _target_: meds_torchdata.MEDSTorchDataConfig
  tensorized_cohort_dir: ???
  max_seq_len: ???
  task_labels_dir: ??? # same as mt_labels_dir: anchors are self-contained in the MT label parquets
  seq_sampling_strategy: to_end

mt_labels_dir: ??? # directory produced by scripts/prepare_multi_task_labels.py
num_tasks: ???
batch_size: 32
num_workers: null
pin_memory: null

# Transformer encoder: 2-layer, 4-head, 128-D hidden, pre-norm.
# query_projector.in_dim must be set to 128.
_target_: medrap.encoders.TransformerPatientEncoder
vocab_size: 65536
embedding_dim: 128
num_heads: 4
num_layers: 2
feedforward_dim: 256
dropout: 0.1
max_seq_len: 512

meds_data_dir: ???
output_dir: ???
tensorized_dir: null # provide to skip stages 1 + 2 (MEDS-transforms + MTD_preprocess)
min_subjects_per_code: 100
min_events_per_subject: 10
num_tasks: 25
horizon_days: 7.0
min_history_days: 1.0
seed: 42
min_positive_count: 10 # min in-window positive subjects for a sampled task code to be kept, on every split
min_positive_rate: 0.01 # min in-window positive rate for a sampled task code to be kept, on every split
max_positive_rate: 0.5 # max in-window positive rate for a sampled task code to be kept, on every split
do_overwrite: false

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
